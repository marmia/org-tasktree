#+TITLE: M4 acceptance test plan (org-tasktree)
#+STARTUP: showall

* Scope
- Target commands: org-tasktree-find-project (new/update path to DB)
- Focus: Editing buffer flow, parse/validation, DB commit for project
- Out of scope: phase/task commit (後続で対応)

* Environment
- fill-column: 100 (設定済 .dir-locals.el)
- Lint: elisp-lint.sh 全ファイル現状パス
- Steps run after code changes: elisp-format.sh → elisp-lint.sh

* Current issues
- TAB/S-TAB ナビと org-read-date ショートカット(C-c C-s/C-c C-d)が未実装
- type/uid の非表示化と内部保持の確認が未実施
- C-c C-k 後にウィンドウが残るケースの再現と修正が必要
- 日付正規化でエラーが出るケースあり (scheduled/deadline)

* Plan (M4 test readiness)
1) find-project end-to-end (new/update)
   - 既存プロジェクト選択→ priority/scheduled/deadline/tags プリフィル（UID/type は非表示で内部保持）
   - TAB/S-TAB で project_name → priority → scheduled → deadline → tags を移動できる
   - `C-c C-s`/`C-c C-d` がそれぞれ scheduled/deadline に org-read-date を開き、初期値は today
   - C-c C-c で DB commit 成功しメッセージ表示、UID は既存なら維持・新規時は採番
2) Parser hardening
   - セパレータ以降だけをパース（実装済み確認）
   - 取得値の trim/prop 削除確認
   - debug メッセージ継続
3) Date handling
   - scheduled/deadline は `org-tasktree-model--valid-date-p` で検証
   - 空文字は nil に正規化
4) Spec alignment for editor
   - 仕様変更: カーソル強制制限は撤廃し、TAB/S-TAB フィールド移動を実装
   - type/uid は非表示（内部保持）。scheduled/deadline の日付入力は org-read-date ショートカットで today 初期値。
   - `C-c C-k` でバッファとウィンドウを確実に閉じる
5) Manual acceptance checklist
   - 新規 project: name= m4new, scheduled=2025-12-14, tags=dev → DB 反映確認（UID/typeは表示されない）
   - 既存 project: m4test を選択し scheduled を org-read-date(C-c C-s) で today→別日変更 → 反映確認、UID 変化しない
   - TAB/S-TAB のフォーカス順が仕様通りか確認
   - C-c C-k 実行後にバッファとウィンドウが残らないこと
   - エラー系: scheduled=invalid → user-error と debug メッセージ

* Deliverables
- Working project create/update via org-tasktree-find-project
- 明文化: TAB/S-TAB ナビ、org-read-date ショートカット、UID/type 非表示、C-c C-k で確実に閉じる仕様
- 記録: debug メッセージ仕様

* Next steps (after project OK)
- 同フレームワークを phase/task に展開
- TAB/S-TAB ナビとショートカットを phase/task に適用、差分があれば記録

* Next session focus
- Add targeted debug logging (line-length気にせず) if date validation or parse issues persist.
- Investigate window cleanup: ensure `C-c C-k` closes buffer+window (consider `quit-window`), reproduce intermittent case.
