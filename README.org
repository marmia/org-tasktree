#+TITLE: README
#+STARTUP: showall

* Project Overview

Development of =org-tasktree=, a task management plugin for Emacs.

* What is org-tasktree?

=org-tasktree= is a task management plugin for Emacs that uses =org-mode= as
the editing UI and SQLite as the single master database.

- Org buffers are editing views.
- SQLite is the single source of truth.
- Tasks are managed in arbitrary parent-child hierarchies.
- domain / project / phase / group can be used as classification tags.

* Features

- Uses only standard =org-mode= features:
  - =org-todo= / =org-schedule= / =org-deadline= / =org-priority= / Org tags
- Fast SQLite-based search
- Safe synchronization design based on UID-based INSERT / UPDATE operations
- Flexible search query definitions using YAML
- Tag search using node_tags

* Requirements

- Emacs 29 or later, with =sqlite.el= support
- =org-mode=, bundled with Emacs
- Supported OS:
  - macOS
  - Linux
- Windows:
  - Not tested

* Installation (Planned)

At this stage, =org-tasktree= is not registered on MELPA.
The expected setup is to clone the source and add it to =load-path=.
Whether it will be registered on MELPA in the future is undecided.

* Initial Setup

On first startup, =org-tasktree= automatically initializes the following
settings relative to =user-emacs-directory=.

- SQLite DB:
  - =~/.emacs.d/org-tasktree/tasktree.db=
  - =~/.config/emacs/org-tasktree/tasktree.db=, etc.

- Query directory:
  - =~/.emacs.d/org-tasktree/queries/=
  - =~/.config/emacs/org-tasktree/queries/=

Run the following command once on first use.

#+begin_src elisp
M-x org-tasktree-init
#+end_src

This automatically performs all of the following:

- Creates the DB file
- Creates the tables
- Creates the query directory

These paths can be freely changed later via =defcustom=.

* Basic Usage

** Create and Register a Task Tree in org-mode

1. Write an arbitrary parent-child hierarchy from scratch in a new Org buffer.
2. Synchronize the entire tree to SQLite.
   - Synchronize it with =org-tasktree-sync-buffer=.

** Load a DB Task Tree into an Org View and Edit It

1. Search for an existing node and open an Org-format editing view.
2. Edit the tree in =org-mode=.
   - Headings with a =uid= update existing nodes; headings without a =uid= are
     treated as new nodes.
3. Resynchronize only the edited range to the DB.
   - Synchronize it with =org-tasktree-sync-subtree= /
     =org-tasktree-sync-region=.

** Quickly Register a Single Node from the UI

1. Run =M-x org-tasktree-find-node=.
2. Follow existing nodes via find-file-style hierarchical completion, then enter
   the new node name at the end.
3. In the opened editing buffer, enter the required fields: priority /
   scheduled / deadline / tags / content. Confirm with =C-c C-c=.
4. When domain / project / phase / group tags are set, they are reflected in
   completion candidate colors.

* Design Policy

- nodes are inserted or updated by UID.
- node_tags are regenerated per node_id using DELETE / INSERT operations.
- Uniqueness is guaranteed by UID.
- parent_id is not stored in Org buffers; it is recalculated from the Org tree.
- Org buffer contents are always temporary views.
- Only DB contents are authoritative.

* License

Undecided; it will be decided after development is complete.

* For Developers

- Project documents are written in Japanese.
- See the following specification:
  - =docs/spec/README.org=
